## SQL Task 3 – Superstore Analysis

This task demonstrates basic SQL skills including:
- Filtering using WHERE
- Sorting using ORDER BY
- Aggregations using SUM, AVG, COUNT
- Grouping using GROUP BY
- Filtering aggregates using HAVING
- Pattern matching using LIKE
- Date filtering using BETWEEN

All queries are saved in queries_task3.sql and results are exported as CSV.
